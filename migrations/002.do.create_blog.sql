CREATE TABLE blog (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  title TEXT NOT NULL,
  preview TEXT NOT NULL,
  post TEXT NOT NULL,
  date_posted TIMESTAMP DEFAULT now() NOT NULL,
  cat_id INTEGER REFERENCES blog_categories(id) NOT NULL
);